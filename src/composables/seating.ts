import { Guest } from '../types';

export const useSeating = () => {
  const rows = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h', 'i', 'j'];

  const assignSeats = (guestsSorted: Guest[]) => {
    
  };

  return { rows };
};